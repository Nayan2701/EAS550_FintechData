fintech_dbt:
  target: dev
  outputs:
    dev:
      type: postgres
      host: localhost
      user: your_db_username
      password: your_db_password
      port: 5432
      dbname: fintech_database
      schema: public 